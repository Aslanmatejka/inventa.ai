[pytest]
testpaths = tests
python_files = test_*.py
# The repo-root test_*.py scripts use sys.exit() and pollute shared state; we
# run them via tests/test_suite.py in subprocesses instead of letting pytest
# auto-collect them at the root.
norecursedirs = Backend client exports migrations node_modules .venv __pycache__
